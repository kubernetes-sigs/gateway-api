apiVersion: internal.gateway.networking.k8s.io/v1alpha1
kind: GEPDetails
number: 5250
name: Multi Stage Routing
status: Provisional
authors:
  - nirrozenbaum
# featureNames is a list of the feature names introduced by the GEP, if there
# are any. This will allow us to track which feature was introduced by which GEP.
featureNames:
  - MultiStageRoute
relationships:
  seeAlso:
    - number: 5091
      name: Payload Processing Resource
      description: Will impact the API design of how request/response are processed in between stages.
    - number: 5224
      name: Pre-Routing Filters
      description: Will impact how to define ordered sequence of filters as part of the payload processing between stages.
