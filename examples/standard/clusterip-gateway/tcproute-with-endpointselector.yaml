kind: TCPRoute
metadata:
  name: service-route
spec:
  parentRefs:
  - name: example-cluster-ip-gateway
  rules:
    config:
	sessionAffinity: false
    backendRefs:
    - kind: EndpointSelector
      port: 8080
      name: front-end-pods
      weight: 100

